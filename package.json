{
  "name": "72-marketplace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate:agents": "node scripts/validate-agents.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
